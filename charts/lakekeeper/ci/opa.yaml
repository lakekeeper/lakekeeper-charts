helmWait: true
internalOpenFGA: true
authn:
  k8s:
    enabled: true
authz:
  backend: "openfga"
OPABridge:
  enabled: true
  lakekeeper:
    tokenEndpoint: "http://localhost/nonexistant/oatuh2/token"
    clientId: "example-client-id"
    clientSecret: "example-client-secret"
